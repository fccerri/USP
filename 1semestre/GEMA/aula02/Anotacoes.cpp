#include <bits/stdc>
int main () {
    int i = 0;

    while (i <5) {
    i++; // soma 1 
    i* = 2; //multiplica i por 2 
    break; //para a repetição
}
    for (int r = 0; r <5; r++)

    int x;
    printf("%.2f", x)

cout.setf(ios::fixed);
cout.precision(2); //define a precisão dos decimais

}